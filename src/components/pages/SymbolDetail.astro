---
import { render } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import Base from '../../layouts/Base.astro';
import { t, type Locale } from '../../i18n/ui';

interface Props {
  locale: Locale;
  entry: CollectionEntry<'symbols'>;
}
const { locale, entry } = Astro.props;
const { Content } = await render(entry);
---

<Base locale={locale} title={entry.data.name}>
  <div class="wrap">
    <section class="py-16">
      <p class="eyebrow mb-3">{t(locale, 'world.symbols')}</p>
      <h1 class="serif text-[clamp(38px,8vw,60px)] leading-tight font-light">{entry.data.name}</h1>
    </section>

    <section class="serif pb-12 text-[clamp(19px,3vw,23px)] italic leading-relaxed">
      <Content />
    </section>

    {
      entry.data.readings.length > 0 && (
        <section class="pb-12">
          <p class="eyebrow mb-6">{t(locale, 'symbol.readings')}</p>
          <p class="serif text-lg text-piedra">{entry.data.readings.join(' · ')}</p>
        </section>
      )
    }

    {
      entry.data.appearances.length > 0 && (
        <section class="pb-20">
          <p class="eyebrow mb-6">{t(locale, 'symbol.appearances')}</p>
          <p class="serif text-lg text-piedra">{entry.data.appearances.join(' · ')}</p>
        </section>
      )
    }
  </div>
</Base>
